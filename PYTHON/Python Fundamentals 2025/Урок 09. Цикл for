🔸🔸🔸 УРОК 9: ЦИКЛ FOR 🔸🔸🔸

📌 ИТЕРИРУЕМЫЙ ОБЪЕКТ
Итерируемый объект (или последовательность) — это объект, который может состоять из множества элементов и предоставлять их по одному, когда это необходимо.
Примером итерируемых объектов являются строки, так как их можно "разбить" на отдельные символы.

📌📌 ЦИКЛ FOR
Цикл `for` позволяет повторно выполнять блок кода (итерацию) для каждого элемента в последовательности.

📌 СИНТАКСИС
```python
for переменная in последовательность:
    # блок кода, выполняющийся для каждого элемента
```
- **Переменная** — принимает значение текущего элемента последовательности на каждой итерации.
- **Последовательность** — итерируемый объект (например, строка), по которому проходит цикл.
- **Блок кода** — выполняется для каждого элемента последовательности.

✅ ПРИМЕР
```python
text = "Python"
for letter in text:
    print(letter)
```

📌📌 ФУНКЦИЯ RANGE
Функция `range()` создаёт последовательность чисел, которую можно использовать в цикле `for`.

📌 СИНТАКСИС
```python
range(start, stop, step)
```
- `start` — начальное значение (по умолчанию 0).
- `stop` — конечное значение (не включается в последовательность).
- `step` — шаг (по умолчанию 1).

✅ ПРИМЕРЫ
```python
for i in range(5):
    print(i)  # 0 1 2 3 4

for i in range(2, 6):
    print(i)  # 2 3 4 5

for i in range(1, 10, 2):
    print(i)  # 1 3 5 7 9

for i in range(10, 0, -2):
    print(i)  # 10 8 6 4 2
```

📌📌 ОПЕРАТОРЫ BREAK, CONTINUE, ELSE В ЦИКЛЕ FOR

📌 ОПЕРАТОР `BREAK`
Прерывает выполнение цикла при достижении определённого условия.
```python
for letter in "Python":
    if letter == "h":
        break
    print(letter)
```

📌 ОПЕРАТОР `CONTINUE`
Пропускает текущую итерацию и переходит к следующей.
```python
for letter in "Python":
    if letter == "h":
        continue
    print(letter)
```

📌 ОПЕРАТОР `ELSE`
Выполняется, если цикл завершился без `break`.
```python
for letter in "Python":
    if letter == "a":
        break
    print(letter)
else:
    print("Цикл завершён нормально.")
```

📌📌 ВЛОЖЕННЫЕ ЦИКЛЫ
Цикл внутри другого цикла.

📌 СИНТАКСИС
```python
for внешняя_переменная in внешняя_последовательность:
    for внутренняя_переменная in внутренняя_последовательность:
        # код, выполняемый внутри обоих циклов
```

✅ ПРИМЕР
```python
for i in "AB":
    for j in "12":
        print(i, j)
```

📌📌 ВЛОЖЕННЫЕ ЦИКЛЫ С WHILE И FOR
Можно использовать комбинации `for` и `while`.
```python
current_hours = int(input("Введите текущий час: "))
hours = 3
end_time = current_hours + 3

while current_hours < 24 and current_hours < end_time:
    for minutes in range(60):
        print(f"Время (часов:минут): {current_hours}:{minutes}")
    current_hours += 1
```

✅✅ ПРАКТИЧЕСКИЕ ЗАДАНИЯ

✅ 1. ФАКТОРИАЛ ЧИСЛА
```python
n = int(input("Введите число: "))
factorial = 1
for i in range(1, n + 1):
    factorial *= i
print(f"Факториал числа {n} равен {factorial}")
```

✅ 2. ЗВЁЗДНЫЙ ПРЯМОУГОЛЬНИК
```python
width = int(input("Введите ширину: "))
height = int(input("Введите высоту: "))
for _ in range(height):
    print("*" * width)
```

✅ 3. ПРОВЕРКА НА ПРОСТОЕ ЧИСЛО
```python
num = int(input("Введите число: "))
if num > 1:
    for i in range(2, num):
        if num % i == 0:
            print("Не является простым")
            break
    else:
        print("Является простым")
else:
    print("Не является простым")
```


