🔸🔸🔸 УРОК 34. ДЕКОРАТОРЫ 🔸🔸🔸

📌 ДЕКОРАТОРЫ
Декораторы позволяют добавлять дополнительное поведение функции без изменения её кода.

🔸 Функции — объекты, их можно передавать и возвращать.
🔸 Декоратор принимает функцию, добавляет логику, возвращает новую функцию.

Как работает:
🔸 Создаётся функция-декоратор, принимающая функцию.
🔸 Внутри создаётся wrapper, выполняющий доп. код и вызывает функцию.
🔸 Возвращается wrapper.

✅
def simple_decorator(func):
    def wrapper():
        print("Перед вызовом функции")
        func()
        print("После вызова функции")
    return wrapper

def say_hello():
    print("Привет!")

say_hello = simple_decorator(say_hello)
say_hello()
🔸 При вызове say_hello() теперь срабатывает wrapper.

📌 СИНТАКСИС @DECORATOR

🔸Можно упростить синтаксис:
✅
@simple_decorator
def say_hello():
    print("Привет!")
    
🔸Эквивалентно:
✅
say_hello = simple_decorator(say_hello)

🔸Пример использования для измерения времени:
✅
import time

def timing_decorator(func):
    def wrapper():
        start = time.time()
        func()
        end = time.time()
        print(f"Время: {end - start:.5f} секунд")
    return wrapper

@timing_decorator
def slow_function():
    time.sleep(2)
    print("Выполнено")

slow_function()

📌 ГДЕ ПРИМЕНЯЮТСЯ ДЕКОРАТОРЫ
🔸 Логирование вызовов
🔸 Измерение времени выполнения
🔸 Ограничение частоты вызова функций
🔸 Проверка входных данных
🔸 Автоматическое повторение при ошибке (retry)
🔸 Ограничение доступа (права пользователя)
🔸 Кеширование результатов
🔸 Автоматическое изменение формата данных

🔸 Позволяют разгружать код функций и добавлять к ним универсальное поведение.

📌 ПОЛЯ КЛАССА
Поле класса (атрибут класса) — общее для всех объектов, хранится внутри класса.

✅
class Book:
    library_name = "Central Library"
🔸 Используется для настроек, счётчиков, категорий.

🔸Чтение:
Book.library_name
book.library_name

🔸Изменение:
Через класс: изменится для всех
Через объект: создаст новое поле в объекте, не меняя поле класса

✅
Book.library_name = "City Library"      # изменится у всех
book.library_name = "Private Shelf"     # изменится только у book

📌 КЛАССОВЫЕ МЕТОДЫ
Классовый метод (@classmethod) работает с классом, а не объектом.
Первый аргумент — cls (ссылка на класс).

Применение:
🔸 Работа с полями класса
🔸 Создание объектов по шаблону
🔸 Поведение, относящееся ко всему классу

Синтаксис:
✅
class Book:
    @classmethod
    def method_name(cls, ...):
        ...
✅
class Book:
    total_books = 0

    def __init__(self, title):
        self.title = title
        Book.total_books += 1

    @classmethod
    def show_total(cls):
        print(f"Total books: {cls.total_books}")

Book.show_total()

📌 СТАТИЧЕСКИЕ МЕТОДЫ
Статический метод (@staticmethod) не зависит ни от объекта, ни от класса.
Не имеет аргументов self, cls.

🔸 Используется для логически связанных с классом функций.

Синтаксис:
✅
class Book:
    @staticmethod
    def method_name(...):
        ...
✅
class MathHelper:
    @staticmethod
    def square(x):
        return x * x
        
📌 СРАВНЕНИЕ МЕТОДОВ

| Тип метода         | Обычный метод             | Классовый метод               | Статический метод                 |
|--------------------|---------------------------|-------------------------------|-----------------------------------|
| Декоратор          | –                         | @classmethod                  | @staticmethod                     |
|                    |                           |                               |                                   |
| Первый аргумент    | self                      | cls                           | –                                 |
|                    |                           |                               |                                   |
| Доступ             | К полям и методам объекта | К полям и методам класса      | –                                 |
|                    |                           |                               |                                   |
| Когда использовать | Когда метод работает      | Когда метод работает с классом| Когда функция логически относится |
|                    | с конкретным экземпляром  | или создаёт объекты по шаблону| к классу, но ничего не использует |
