🔸🔸🔸 УРОК 5. ОПЕРАТОРЫ ВЕТВЛЕНИЯ 🔸🔸🔸 

📌 ЧТО ТАКОЕ ОПЕРАТОРЫ ВЕТВЛЕНИЯ
Операторы ветвления позволяют выполнять разные блоки кода в зависимости от условий.
Они помогают программе принимать решения.

В Python используются:
🔸if — проверяет условие и выполняет блок, если условие True.
🔸elif — проверяет другое условие, если предыдущие не выполнились.
🔸else — выполняется, если все условия False.

Это ключевые слова, их нельзя использовать как имена переменных.

📌 ОПЕРАТОР IF
Синтаксис:
Оператор if выполняет блок кода, если условие истинно (True).
✅
if условие:
    # код, если условие True
Если условие ложно (False), код под if не выполняется.

✅
x = 10
if x > 5:
    print("x больше 5")  # выполнится

📌 НЕЯВНОЕ ПРЕОБРАЗОВАНИЕ В BOOL
В if любое значение преобразуется в True или False:

0, 0.0, '', [], {}, None → False

Все остальные значения → True
✅
a = 5
if a:
    print("a воспринимается как True")

📌 ОПЕРАТОР ELIF
elif (else if) позволяет проверить альтернативные условия.
✅
if условие1:
    # если условие1 True
elif условие2:
    # если условие2 True, когда условие1 False
elif условие3:
    # если условие3 True, когда предыдущие False
✅
x = 7
if x > 10:
    print("x больше 10")
elif x > 5:
    print("x больше 5, но меньше или равно 10")

📌 ОПЕРАТОР ELSE
else выполняется, если все условия выше были False.
Синтаксис:
✅
if условие:
    # если True
elif другое_условие:
    # если True
else:
    # если все False
✅
x = 3
if x > 5:
    print("x больше 5")
else:
    print("x не больше 5")

📌 ИСПОЛЬЗОВАНИЕ НЕСКОЛЬКИХ IF
Можно использовать несколько независимых if, если условия не связаны:
✅
x = 7
if x > 5:
    print("x больше 5")
if x < 10:
    print("x меньше 10")
if x == 7:
    print("x равно 7")
✅ Все условия проверяются независимо.

📌 ВЛОЖЕННЫЕ УСЛОВИЯ
Можно вкладывать if в if, если проверка следующего условия зависит от предыдущего.
✅
age = 25
if age >= 18:
    print("Совершеннолетний")
    if age < 30:
        print("Молодой взрослый")

📌 РАВНОСИЛЬНЫЙ КОД БЕЗ ВЛОЖЕННОСТИ
Вместо вложенных условий можно использовать and и or для упрощения кода:
✅
age = 25
if age >= 18 and age < 30:
    print("Молодой взрослый")
✅ Делает код более линейным и читаемым.

📌 ТЕРНАРНЫЙ ОПЕРАТОР
Позволяет записать условие в одну строку.
✅
результат = "Да" if условие else "Нет"
✅
age = 18
status = "Взрослый" if age >= 18 else "Несовершеннолетний"
print(status)

📌 КОНСТРУКЦИЯ MATCH-CASE
Введена в Python 3.10, удобна для проверки одного значения на множество условий.

Синтаксис:
✅
match переменная:
    case значение1:
        # действия
    case значение2:
        # действия
    case _:
        # если ничего не совпало
✅
number = 7
match number:
    case 1:
        print("Один")
    case 2 | 3:
        print("Два или три")
    case _ if number > 5:
        print("Число больше 5")
    case _:
        print("Неизвестное число")

📌 РАБОТА С ТИПАМИ ДАННЫХ В MATCH-CASE
Можно использовать match-case для проверки типов данных:
✅
value = "Hello"
match value:
    case int():
        print("Это число")
    case str():
        print("Это строка")
    case _:
        print("Неизвестный тип")



Запитати ChatGPT


