🔸🔸🔸 ОСНОВНЫЕ МЕТОДЫ ДЛЯ РАБОТЫ С ЧИСЛАМИ 🔸🔸🔸

📌 1. ОПЕРАЦИИ С ЧИСЛАМИ
✅ 
x = -10
y = 3.14159

print(abs(x))        # 10 - модуль числа
print(pow(2, 3))     # 8 - возведение в степень (2^3)
print(round(y, 2))   # 3.14 - округление до 2 знаков
print(divmod(10, 3)) # (3, 1) - частное и остаток (10 // 3, 10 % 3)

📌 2. ПРЕОБРАЗОВАНИЕ ТИПОВ
✅ 
print(int(3.9))     # 3 - отбрасывает дробную часть
print(float(10))    # 10.0 - перевод в `float`
print(str(42))      # '42' - преобразование в строку
print(bin(10))      # '0b1010' - двоичное представление
print(oct(10))      # '0o12' - восьмеричное представление
print(hex(10))      # '0xa' - шестнадцатеричное представление

📌 3. МАТЕМАТИЧЕСКИЕ ФУНКЦИИ (из модуля math)
✅ import math

z = 16

print(math.sqrt(z))      # 4.0 - квадратный корень
print(math.ceil(y))      # 4 - округление вверх
print(math.floor(y))     # 3 - округление вниз
print(math.factorial(5)) # 120 - факториал 5! = 5*4*3*2*1
print(math.gcd(54, 24))  # 6 - наибольший общий делитель
print(math.lcm(5, 3))    # 15 - наименьшее общее кратное (Python 3.9+)
print(math.log(8, 2))    # 3.0 - логарифм (лог₂ 8)
print(math.exp(2))       # 7.389 - e^2
print(math.sin(math.pi/2))  # 1.0 - синус 90 градусов

📌 4. РАБОТА С DECIMAL (ДЛЯ ТОЧНЫХ ВЫЧИСЛЕНИЙ)

✅ from decimal import Decimal, getcontext

getcontext().prec = 5  # Устанавливаем точность до 5 знаков

a = Decimal("10.12345")
b = Decimal("3.14")

c = a + b  # Decimal('13.263')
print(c)

📌 5. РАБОТА С FRACTIONS (ДРОБИ)

✅ from fractions import Fraction

frac1 = Fraction(3, 4)  # 3/4
frac2 = Fraction(2, 3)  # 2/3

result = frac1 + frac2  # 17/12
print(result)







