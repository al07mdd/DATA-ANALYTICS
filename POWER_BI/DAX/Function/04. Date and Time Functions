📌 Функции даты и времени позволяют создавать вычисления на основе дат и времени.
Многие функции в DAX аналогичны функциям даты и времени в Excel. 📌

✅ 
| Функция      | Описание (перевод                                                                                                                                                         |
| ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| CALENDAR     | Возвращает таблицу с одним столбцом всех дат между StartDate и EndDate.                                                                                                   |
| CALENDARAUTO | Возвращает таблицу с одним столбцом дат, автоматически рассчитанных из модели.                                                                                            |
| DATE         | Возвращает указанную дату в формате datetime.                                                                                                                             |
| DATEDIFF     | Возвращает количество единиц (указанных в параметре Interval) между двумя датами.                                                                                         |
| DATEVALUE    | Преобразует дату в текстовом виде в дату в формате datetime.                                                                                                              |
| DAY          | Возвращает число от 1 до 31, представляющее день месяца.                                                                                                                  |
| EDATE        | Возвращает дату, которая на указанное количество месяцев раньше или позже даты начала.                                                                                    |
| EOMONTH      | Возвращает дату последнего дня месяца в формате datetime, смещенную на указанное количество месяцев.                                                                      |
| HOUR         | Возвращает час как число от 0 (00:00) до 23 (23:00).                                                                                                                      |
| MINUTE       | Возвращает число от 0 до 59, представляющее минуты.                                                                                                                       |
| MONTH        | Возвращает число от 1 (январь) до 12 (декабрь), представляющее месяц.                                                                                                     |
| NETWORKDAYS  | Возвращает количество полных рабочих дней между двумя датами (включительно), с учетом параметров выходных и праздников. Выходные и праздники не считаются рабочими днями. |
| NOW          | Возвращает текущие дату и время в формате datetime.                                                                                                                       |
| QUARTER      | Возвращает число от 1 (январь-март) до 4 (октябрь-декабрь), представляющее квартал.                                                                                       |
| SECOND       | Возвращает число от 0 до 59, представляющее секунды.                                                                                                                      |
| TIME         | Преобразует часы, минуты и секунды, заданные как числа, во время в формате datetime.                                                                                      |
| TIMEVALUE    | Преобразует время в текстовом виде во время в формате datetime.                                                                                                           |
| TODAY        | Возвращает текущую дату в формате datetime.                                                                                                                               |
| UTCNOW       | Возвращает текущие дату и время в формате datetime в формате Coordinated Universal Time (UTC).                                                                            |
| UTCTODAY     | Возвращает текущую дату в формате datetime в формате Coordinated Universal Time (UTC).                                                                                    |
| WEEKDAY      | Возвращает число, определяющее день недели для даты. Диапазон значений от 1 до 7 или от 0 до 6 в зависимости от параметра ReturnType.                                     |
| WEEKNUM      | Возвращает номер недели в году.                                                                                                                                           |
| YEAR         | Возвращает год из даты в виде четырехзначного числа.                                                                                                                      |
| YEARFRAC     | Возвращает долю года, представляющую количество полных дней между start\_date и end\_date.                                                                                |

