📌 Функции фильтра управляют контекстами таблиц и фильтров. 📌

✅
| Функция          | Описание (перевод                                                                                                                                                                                                                                                                                                                    |
| ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| ALL              | Возвращает все строки в таблице или все значения в столбце, игнорируя примененные фильтры.                                                                                                                                                                                                                                           |
| ALLCROSSFILTERED | Очищает все фильтры, примененные к указанной таблице.                                                                                                                                                                                                                                                                                |
| ALLEXCEPT        | Возвращает все строки в таблице, кроме строк, затронутых фильтрами указанных столбцов.                                                                                                                                                                                                                                               |
| ALLNOBLANKROW    | Возвращает все строки, кроме пустой строки в таблице, или все значения в столбце, игнорируя примененные фильтры.                                                                                                                                                                                                                     |
| ALLSELECTED      | Возвращает все строки в таблице или все значения в столбце, игнорируя фильтры внутри запроса, но сохраняя фильтры, пришедшие извне.                                                                                                                                                                                                  |
| CALCULATE        | Вычисляет выражение в контексте, измененном фильтрами.                                                                                                                                                                                                                                                                               |
| CALCULATETABLE   | Вычисляет табличное выражение в контексте, измененном фильтрами.                                                                                                                                                                                                                                                                     |
| FILTER           | Возвращает таблицу, к которой применен фильтр.                                                                                                                                                                                                                                                                                       |
| INDEX            | Возвращает строку по абсолютной позиции (указанной параметром position) в пределах указанной области (partition), отсортированной по заданному порядку или оси.                                                                                                                                                                      |
| KEEPFILTERS      | Изменяет семантику фильтрации функций CALCULATE и CALCULATETABLE, сохраняя существующие фильтры.                                                                                                                                                                                                                                     |
| LOOKUP           | Находит ячейки, где указанные столбцы совпадают с вычисленными выражениями, и извлекает значение столбца (если найдена только одна ячейка) или вычисляет значение выражения из этих совпадающих строк. Контекст фильтра из неуказанных столбцов применяется неявно.                                                                  |
| LOOKUPVALUE      | Извлекает значение из таблицы.                                                                                                                                                                                                                                                                                                       |
| LOOKUPWITHTOTALS | Находит ячейки, где указанные столбцы совпадают с вычисленными выражениями, и извлекает значение столбца (если найдена только одна ячейка) или вычисляет значение выражения из совпадающих строк. По умолчанию устанавливает неуказанные столбцы в "итого", в отличие от LOOKUP, который не изменяет фильтр по неуказанным столбцам. |
| MATCHBY          | Столбцы, используемые для определения соответствия данных и идентификации текущей строки. Используется только в оконных функциях (Window functions).                                                                                                                                                                                 |
| OFFSET           | Извлекает одну строку из набора, перемещаясь на заданное количество строк в пределах указанной области (partition), отсортированной по указанному порядку или оси.                                                                                                                                                                   |
| ORDERBY          | Выражения и направления сортировки, используемые для определения порядка сортировки в пределах каждой области (partition). Используется только в оконных функциях.                                                                                                                                                                   |
| PARTITIONBY      | Столбцы, используемые для определения области разбиения данных. Используется только в оконных функциях.                                                                                                                                                                                                                              |
| REMOVEFILTERS    | Удаляет фильтры из указанных таблиц или столбцов.                                                                                                                                                                                                                                                                                    |
| SELECTEDVALUE    | Возвращает значение, если в указанном столбце только одно значение, в противном случае возвращает альтернативный результат.                                                                                                                                                                                                          |
| WINDOW           | Извлекает диапазон строк в пределах указанной области (partition), отсортированной по заданному порядку или оси.                                                                                                                                                                                                                     |
