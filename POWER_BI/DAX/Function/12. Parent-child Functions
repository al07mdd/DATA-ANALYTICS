📌 Эти функции помогают преобразовать иерархию «родители-потомки» в обычную иерархию. 📌

✅
| Функция         | Описание (перевод)                                                                                                                  |
| --------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| PATH            | Возвращает строку с разделенным списком идентификаторов, начиная с вершины (корня) иерархии и заканчивая указанным идентификатором. |
| PATHCONTAINS    | Возвращает TRUE, если указанный элемент содержится в указанном пути.                                                                |
| PATHITEM        | Возвращает n-й элемент в списке, сформированном функцией PATH.                                                                      |
| PATHITEMREVERSE | Возвращает n-й элемент в списке, сформированном функцией PATH, считая с конца.                                                      |
| PATHLENGTH      | Возвращает количество элементов в строке пути. Для корня иерархии возвращает 1.                                                     |
