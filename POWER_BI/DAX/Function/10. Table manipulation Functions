📌 Эти функции манипулируют таблицами и возвращают их. 📌

✅
| Функция                   | Описание (перевод)                                                                                                                                                                  |
| ------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ADDCOLUMNS                | Возвращает таблицу с новыми столбцами, вычисленными по DAX-выражениям.                                                                                                              |
| ADDMISSINGITEMS           | Добавляет обратно строки с пустыми значениями мер.                                                                                                                                  |
| CROSSJOIN                 | Возвращает таблицу, являющуюся декартовым произведением указанных таблиц.                                                                                                           |
| CURRENTGROUP              | Доступ к подтаблице текущей группы в функции GROUPBY (используется только внутри GROUPBY).                                                                                          |
| DATATABLE                 | Возвращает таблицу с данными, определенными непосредственно в формуле.                                                                                                              |
| DETAILROWS                | Возвращает таблицу, соответствующую DetailRows выражению меры, либо всю таблицу, к которой принадлежит мера.                                                                        |
| DISTINCT                  | Возвращает таблицу с одним столбцом уникальных значений или несколько столбцов с уникальными комбинациями значений.                                                                 |
| EXCEPT                    | Возвращает строки из левой таблицы, которых нет в правой таблице.                                                                                                                   |
| FILTERS                   | Возвращает таблицу значений фильтра, примененного непосредственно к указанному столбцу.                                                                                             |
| GENERATE                  | Вычисляет второе табличное выражение для каждой строки первой таблицы и возвращает их декартово произведение.                                                                       |
| GENERATEALL               | Аналогично GENERATE, но включает строки, для которых второе выражение возвращает пустое значение.                                                                                   |
| GENERATESERIES            | Возвращает таблицу с одним столбцом, заполненным последовательными значениями от start до end.                                                                                      |
| GROUPBY                   | Создает сводную таблицу, группируя входную таблицу по указанным столбцам.                                                                                                           |
| IGNORE                    | Отмечает выражение меры, переданное в SUMMARIZECOLUMNS, для игнорирования при определении непустых строк.                                                                           |
| INTERSECT                 | Возвращает строки из левой таблицы, которые также присутствуют в правой таблице.                                                                                                    |
| NATURALINNERJOIN          | Выполняет Inner Join (естественное соединение) между двумя таблицами.                                                                                                               |
| NATURALJOINUSAGE          | Добавляет таблицу как Natural Join в фильтр-контекст.                                                                                                                               |
| NATURALLEFTOUTERJOIN      | Выполняет Left Outer Join (естественное соединение) между двумя таблицами.                                                                                                          |
| NONVISUAL                 | Помечает фильтр как невизуальный.                                                                                                                                                   |
| ROLLUP                    | Выделяет подмножество столбцов в SUMMARIZE для вычисления подытогов.                                                                                                                |
| ROLLUPADDISSUBTOTAL       | Выделяет подмножество столбцов в SUMMARIZECOLUMNS для вычисления групп подытогов.                                                                                                   |
| ROLLUPGROUP               | Выделяет подмножество столбцов в SUMMARIZE для группировки подытогов.                                                                                                               |
| ROLLUPISSUBTOTAL          | Связывает группы rollup с колонкой, добавленной ROLLUPADDISSUBTOTAL.                                                                                                                |
| ROW                       | Возвращает таблицу с одной строкой и новыми столбцами по DAX-выражениям.                                                                                                            |
| SAMPLEAXISWITHLOCALMINMAX | Возвращает подвыборку из таблицы, формируя равные бины по X и сохраняя локальные min/max по сериям.                                                                                 |
| SELECTCOLUMNS             | Возвращает таблицу с выбранными столбцами из таблицы и новыми столбцами по DAX-выражениям.                                                                                          |
| SUBSTITUTEWITHINDEX       | Возвращает таблицу как семисоединение двух таблиц, заменяя общие столбцы индексом, основанным на сортировке второй таблицы.                                                         |
| SUMMARIZE                 | Создает сводную таблицу, группируя входную таблицу по указанным столбцам.                                                                                                           |
| SUMMARIZECOLUMNS          | Создает сводную таблицу для вычисления итогов по группам.                                                                                                                           |
| TOPN                      | Возвращает указанное количество верхних строк по выражению сортировки.                                                                                                              |
| TOPNPERLEVEL              | (Описание отсутствует, требуется уточнение)                                                                                                                                         |
| TOPNSKIP                  | Возвращает строки, эффективно пропуская указанное количество строк. Быстрее, но менее гибкая версия TOPN.                                                                           |
| TREATAS                   | Интерпретирует столбцы входной таблицы как столбцы других таблиц, фильтруя значения, отсутствующие в целевых столбцах.                                                              |
| UNION                     | Возвращает объединение таблиц с совпадающими столбцами.                                                                                                                             |
| VALUES                    | При указании имени столбца возвращает таблицу уникальных значений. При указании имени таблицы возвращает все строки и столбцы таблицы (включая пустую строку при невалидной связи). |
