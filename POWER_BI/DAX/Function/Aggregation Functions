📌 Агрегатные функции возвращают скалярное значение, применяя агрегатную функцию к столбцу
или к выражению, вычисляемому путем итерации табличного выражения. 📌

✅
| Функция                  | Описание                                                                                                                                                                                                                                |
| ------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| APPROXIMATEDISTINCTCOUNT | Возвращает оценочное количество уникальных значений в столбце. Вызывает соответствующую агрегацию в источнике данных, оптимизированную для производительности запросов с небольшой потерей точности. Используется только в DirectQuery. |
| AVERAGE                  | Возвращает среднее (арифметическое) всех чисел в столбце.                                                                                                                                                                               |
| AVERAGEA                 | Возвращает среднее (арифметическое) значений в столбце, учитывая текст и нечисловые значения.                                                                                                                                           |
| AVERAGEX                 | Вычисляет среднее (арифметическое) набора выражений, рассчитанных по таблице.                                                                                                                                                           |
| COUNT                    | Считает количество строк в таблице, где указанный столбец содержит ненулевое значение.                                                                                                                                                  |
| COUNTA                   | Считает количество значений в столбце.                                                                                                                                                                                                  |
| COUNTAX                  | Считает количество значений, полученных в результате вычисления выражения для каждой строки таблицы.                                                                                                                                    |
| COUNTBLANK               | Считает количество пустых значений в столбце.                                                                                                                                                                                           |
| COUNTROWS                | Считает количество строк в таблице.                                                                                                                                                                                                     |
| COUNTX                   | Считает количество значений, полученных в результате вычисления выражения для каждой строки таблицы.                                                                                                                                    |
| DISTINCTCOUNT            | Считает количество различных значений в столбце.                                                                                                                                                                                        |
| DISTINCTCOUNTNOBLANK     | Считает количество различных значений в столбце.                                                                                                                                                                                        |
| MAX                      | Возвращает наибольшее значение в столбце или большее значение между двумя скалярными выражениями. Игнорирует логические значения. Строки сравниваются по алфавиту.                                                                      |
| MAXA                     | Возвращает наибольшее значение в столбце, не игнорируя логические значения и текст.                                                                                                                                                     |
| MAXX                     | Возвращает наибольшее значение, полученное в результате вычисления выражения для каждой строки таблицы. Строки сравниваются по алфавиту.                                                                                                |
| MIN                      | Возвращает наименьшее значение в столбце или меньшее значение между двумя скалярными выражениями. Игнорирует логические значения. Строки сравниваются по алфавиту.                                                                      |
| MINA                     | Возвращает наименьшее значение в столбце, не игнорируя логические значения и текст.                                                                                                                                                     |
| MINX                     | Возвращает наименьшее значение, полученное в результате вычисления выражения для каждой строки таблицы. Строки сравниваются по алфавиту.                                                                                                |
| PRODUCT                  | Возвращает произведение значений указанного столбца.                                                                                                                                                                                    |
| PRODUCTX                 | Возвращает произведение значений выражения в таблице.                                                                                                                                                                                   |
| SUM                      | Суммирует все числа в столбце.                                                                                                                                                                                                          |
| SUMX                     | Возвращает сумму выражений, вычисленных для каждой строки таблицы.                                                                                                                                                                      |
