📌 Это особые функции, которые нельзя отнести к другим категориям. 📌

✅
| Функция                      | Описание (перевод)                                                                                                                  |
| ---------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| BITLSHIFT                    | Возвращает число, сдвинутое влево на указанное количество бит.                                                                      |
| BITRSHIFT                    | Возвращает число, сдвинутое вправо на указанное количество бит.                                                                     |
| BLANK                        | Возвращает пустое значение.                                                                                                         |
| EARLIER                      | Возвращает значение в столбце на предыдущем этапе сканирования таблицы (по умолчанию 1 шаг назад).                                  |
| EARLIEST                     | Возвращает значение в столбце на самом первом этапе, когда был доступен строковый контекст.                                         |
| ERROR                        | Генерирует указанную пользователем ошибку.                                                                                          |
| EXTERNALMEASURE              | Вызывает меру, определенную в удаленной модели.                                                                                     |
| HASH                         | Вычисляет хеш по переменному количеству входных выражений и возвращает значение.                                                    |
| INFO.STORAGETABLES           | Возвращает статистику по in-memory таблицам.                                                                                        |
| ISATLEVEL                    | Показывает, присутствует ли столбец на текущем уровне иерархии.                                                                     |
| KEYWORDMATCH                 | Возвращает TRUE, если есть совпадение между выражением для сопоставления и текстом.                                                 |
| RANK                         | Возвращает ранг для текущего контекста в указанной области (partition), отсортированной по заданному порядку или оси.               |
| ROWNUMBER                    | Возвращает уникальный ранг для текущего контекста в указанной области (partition), отсортированной по заданному порядку или оси.    |
| SAMPLECARTESIANPOINTSBYCOVER | Возвращает выборку из таблицы путем построения точек в 2D пространстве на основе координат и радиусов, удаляя пересекающиеся точки. |
