📌 Статистические функции агрегации. 📌

✅
| Функция         | Описание (перевод)                                                                                                                                       |
| --------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| BETA.DIST       | Возвращает бета-распределение, используется для изучения вариации процентов чего-либо по выборкам.                                                       |
| BETA.INV        | Возвращает обратную величину функции бета-распределения (BETA.DIST). Используется в планировании проектов для моделирования вероятных сроков выполнения. |
| CHISQ.DIST      | Возвращает хи-квадрат распределение, используется для изучения вариации процентов чего-либо по выборкам.                                                 |
| CHISQ.DIST.RT   | Возвращает правостороннюю вероятность хи-квадрат распределения, используется для сравнения наблюдаемых и ожидаемых значений в тестах гипотез.            |
| CHISQ.INV       | Возвращает обратную левостороннюю вероятность хи-квадрат распределения.                                                                                  |
| CHISQ.INV.RT    | Возвращает обратную правостороннюю вероятность хи-квадрат распределения, используется для проверки гипотез по наблюдаемым и ожидаемым значениям.         |
| COMBIN          | Возвращает количество комбинаций для заданного количества элементов.                                                                                     |
| COMBINA         | Возвращает количество комбинаций с повторениями для заданного количества элементов.                                                                      |
| CONFIDENCE.NORM | Возвращает доверительный интервал для среднего генеральной совокупности с использованием нормального распределения.                                      |
| CONFIDENCE.T    | Возвращает доверительный интервал для среднего генеральной совокупности с использованием распределения Стьюдента.                                        |
| EXPON.DIST      | Возвращает экспоненциальное распределение, используется для моделирования времени между событиями.                                                       |
| GEOMEAN         | Возвращает геометрическое среднее для указанного столбца.                                                                                                |
| GEOMEANX        | Возвращает геометрическое среднее выражения, вычисляемого по таблице.                                                                                    |
| LINEST          | Вычисляет линию наименьших квадратов, возвращает таблицу с описанием линии.                                                                              |
| LINESTX         | Вычисляет линию наименьших квадратов для выражений, вычисляемых по каждой строке таблицы.                                                                |
| MEDIAN          | Возвращает медиану (50-й процентиль) значений в столбце.                                                                                                 |
| MEDIANX         | Возвращает медиану выражения, вычисляемого по таблице.                                                                                                   |
| NORM.DIST       | Возвращает значение нормального распределения для заданных среднего и стандартного отклонения.                                                           |
| NORM.INV        | Возвращает обратное значение функции нормального распределения для заданных среднего и стандартного отклонения.                                          |
| NORM.S.DIST     | Возвращает стандартное нормальное распределение (среднее = 0, стандартное отклонение = 1).                                                               |
| NORM.S.INV      | Возвращает обратное значение стандартного нормального распределения.                                                                                     |
| PERCENTILE.EXC  | Возвращает k-й (исключающий) процентиль значений в столбце.                                                                                              |
| PERCENTILE.INC  | Возвращает k-й (включающий) процентиль значений в столбце.                                                                                               |
| PERCENTILEX.EXC | Возвращает k-й (исключающий) процентиль выражения, вычисляемого по таблице.                                                                              |
| PERCENTILEX.INC | Возвращает k-й (включающий) процентиль выражения, вычисляемого по таблице.                                                                               |
| PERMUT          | Возвращает количество перестановок для заданного количества объектов, где важен порядок элементов.                                                       |
| POISSON.DIST    | Возвращает распределение Пуассона, используется для прогнозирования количества событий за определенный промежуток времени.                               |
| RANK.EQ         | Возвращает ранг числа в столбце. При равных значениях возвращается наивысший ранг.                                                                       |
| RANKX           | Возвращает ранг выражения в текущем контексте среди значений выражения, вычисляемого по каждой строке таблицы.                                           |
| SAMPLE          | Возвращает случайную подвыборку из указанного табличного выражения.                                                                                      |
| STDEV.P         | Вычисляет стандартное отклонение для всей генеральной совокупности, игнорируя текст и логические значения.                                               |
| STDEV.S         | Вычисляет стандартное отклонение по выборке, игнорируя текст и логические значения.                                                                      |
| STDEVX.P        | Вычисляет стандартное отклонение для всей генеральной совокупности по выражению, вычисляемому по каждой строке таблицы.                                  |
| STDEVX.S        | Вычисляет стандартное отклонение по выборке по выражению, вычисляемому по каждой строке таблицы.                                                         |
| T.DIST          | Возвращает левостороннее распределение Стьюдента.                                                                                                        |
| T.DIST.2T       | Возвращает двустороннее распределение Стьюдента.                                                                                                         |
| T.DIST.RT       | Возвращает правостороннее распределение Стьюдента.                                                                                                       |
| T.INV           | Возвращает обратное левостороннее распределение Стьюдента.                                                                                               |
| T.INV.2T        | Возвращает обратное двустороннее распределение Стьюдента.                                                                                                |
| VAR.P           | Вычисляет дисперсию для всей генеральной совокупности, игнорируя текст и логические значения.                                                            |
| VAR.S           | Вычисляет дисперсию по выборке, игнорируя текст и логические значения.                                                                                   |
| VARX.P          | Вычисляет дисперсию для всей генеральной совокупности по выражению, вычисляемому по каждой строке таблицы.                                               |
| VARX.S          | Вычисляет дисперсию по выборке по выражению, вычисляемому по каждой строке таблицы.                                                                      |
