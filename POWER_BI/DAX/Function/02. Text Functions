📌 Текстовые функции манипулируют строками. 📌

✅
| Функция       | Описание (перевод                                                                                                                       |
| ------------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| COMBINEVALUES | Объединяет заданный набор операндов, используя указанный разделитель.                                                                   |
| CONCATENATE   | Объединяет две текстовые строки в одну.                                                                                                 |
| CONCATENATEX  | Вычисляет выражение для каждой строки таблицы, затем возвращает объединение этих значений в одной строке с указанным разделителем.      |
| EXACT         | Проверяет, идентичны ли две текстовые строки, возвращает TRUE или FALSE. Чувствителен к регистру.                                       |
| FIND          | Возвращает позицию, с которой начинается одна текстовая строка внутри другой. Чувствителен к регистру и диакритике.                     |
| FIXED         | Округляет число до указанного количества знаков после запятой и возвращает результат в виде текста с возможными запятыми.               |
| FORMAT        | Преобразует значение в текст с указанным числовым форматом.                                                                             |
| LEFT          | Возвращает указанное количество символов с начала текстовой строки.                                                                     |
| LEN           | Возвращает количество символов в текстовой строке.                                                                                      |
| LOWER         | Преобразует все буквы в текстовой строке в нижний регистр.                                                                              |
| MID           | Возвращает строку символов из середины текстовой строки, начиная с указанной позиции и длиной, указанной в параметрах.                  |
| REPLACE       | Заменяет часть текстовой строки другой текстовой строкой.                                                                               |
| REPT          | Повторяет текст указанное количество раз. Используется для заполнения ячейки повторяющимся текстом.                                     |
| RIGHT         | Возвращает указанное количество символов с конца текстовой строки.                                                                      |
| SEARCH        | Возвращает позицию, с которой начинается одна текстовая строка внутри другой. Не чувствителен к регистру, но чувствителен к диакритике. |
| SUBSTITUTE    | Заменяет существующий текст новым текстом в текстовой строке.                                                                           |
| TOCSV         | Преобразует записи таблицы в текст в формате CSV (значения, разделенные запятыми).                                                      |
| TOJSON        | Преобразует записи таблицы в текст в формате JSON.                                                                                      |
| TRIM          | Удаляет все пробелы из текстовой строки, кроме одиночных пробелов между словами.                                                        |
| UNICHAR       | Возвращает Unicode-символ, который соответствует заданному числовому значению.                                                          |
| UNICODE       | Возвращает число (кодовую точку), соответствующую первому символу текстовой строки.                                                     |
| UPPER         | Преобразует текстовую строку в верхний регистр.                                                                                         |
| VALUE         | Преобразует текстовую строку, представляющую число, в число.                                                                            |
