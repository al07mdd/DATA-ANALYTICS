📌 Логические функции воздействуют на выражение, возвращая информацию о значениях или наборах в выражении. 📌

✅
| Функция     | Описание (перевод)                                                                                                                                     |
| ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| AND         | Проверяет, являются ли все аргументы TRUE, и возвращает TRUE, если все аргументы равны TRUE.                                                           |
| BITAND      | Возвращает побитовое 'AND' двух чисел.                                                                                                                 |
| BITOR       | Возвращает побитовое 'OR' двух чисел.                                                                                                                  |
| BITXOR      | Возвращает побитовое 'XOR' двух чисел.                                                                                                                 |
| COALESCE    | Возвращает первый аргумент, который не является пустым значением. Если все аргументы пустые, возвращается BLANK.                                       |
| FALSE       | Возвращает логическое значение FALSE.                                                                                                                  |
| IF          | Проверяет, выполняется ли условие, и возвращает одно значение, если TRUE, и другое, если FALSE.                                                        |
| IF.EAGER    | Проверяет, выполняется ли условие, и возвращает одно значение, если TRUE, и другое, если FALSE. Использует план выполнения с нетерпеливым вычислением. |
| IFERROR     | Возвращает value\_if\_error, если первое выражение является ошибкой, в противном случае возвращает значение самого выражения.                          |
| NOT         | Преобразует FALSE в TRUE, а TRUE в FALSE.                                                                                                              |
| OR          | Возвращает TRUE, если любой из аргументов равен TRUE, и возвращает FALSE, если все аргументы равны FALSE.                                              |
| SWITCH      | Возвращает различные результаты в зависимости от значения выражения.                                                                                   |
| TRUE        | Возвращает логическое значение TRUE.                                                                                                                   |
