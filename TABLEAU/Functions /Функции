📌 A – Функции Tableau 📌

| Функция   |   Синтаксис                      |   Вывод                 |   Определение (RU                                                           |   Пример                                                  |   Примечания (RU                                               |
| --------- | -------------------------------- | ----------------------- | --------------------------------------------------------------------------- | --------------------------------------------------------- | --------------------------------------------------------------- |
| ABS       | `ABS(number)`                    | Число (положительное)   | Возвращает абсолютное значение числа                                        | `ABS(-7) = 7`<br>`ABS([Budget Variance])`                 | См. также: `SIGN`                                               |
| ACOS      | `ACOS(number)`                   | Число (угол в радианах) | Возвращает арккосинус числа                                                 | `ACOS(-1) = 3.1415...`                                    | Обратная: `COS`                                                 |
| AND       | `<expr1> AND <expr2>`            | Boolean                 | Логическое И, возвращает TRUE если оба выражения истинны                    | `IF [Season]="Spring" AND [Season]="Fall" THEN "..." END` | Использует короткую оценку условий; см. также `OR`, `NOT`       |
| AREA      | `AREA(Spatial Polygon, 'units')` | Число                   | Возвращает площадь пространственного полигона                               | `AREA([Geometry], 'feet')`                                | Поддерживаемые единицы: 'meters', 'kilometers', 'miles', 'feet' |
| ASCII     | `ASCII(string)`                  | Число                   | Возвращает ASCII-код первого символа строки                                 | `ASCII('A') = 65`                                         | Обратная: `CHAR`                                                |
| ASIN      | `ASIN(number)`                   | Число (угол в радианах) | Возвращает арксинус числа                                                   | `ASIN(1) = 1.5707...`                                     | Обратная: `SIN`                                                 |
| ATAN      | `ATAN(number)`                   | Число (угол в радианах) | Возвращает арктангенс числа                                                 | `ATAN(180) = 1.5652...`                                   | Обратная: `TAN`; см. также `ATAN2`, `COT`                       |
| ATAN2     | `ATAN2(y, x)`                    | Число (угол в радианах) | Возвращает арктангенс между y и x                                           | `ATAN2(2, 1) = 1.1071...`                                 | См. также `ATAN`, `TAN`, `COT`                                  |
| ATTR      | `ATTR(expression)`               | Значение или `*`        | Возвращает значение выражения, если оно одинаково для всех строк, иначе `*` |                                                           | Null игнорируются                                               |
| AVG       | `AVG(expression)`                | Число                   | Возвращает среднее всех значений выражения                                  |                                                           | Используется только с числовыми полями                          |

📌 B – Функции Tableau 📌

| Функция   | **Синтаксис**                              | **Вывод** | **Определение (RU)**                                                                                 | **Пример**                                                                                   | **Примечания (RU)**                                             |
| --------- | ------------------------------------------ | --------- | ---------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------- | --------------------------------------------------------------- |
| BUFFER    | `BUFFER(Spatial Point, distance, 'units')` | Geometry  | Возвращает полигон вокруг указанной пространственной точки с радиусом, определяемым distance и units | `BUFFER([Spatial Point Geometry], 25, 'mi')`<br>`BUFFER(MAKEPOINT(47.59, -122.32), 3, 'km')` | Поддерживаемые единицы: 'meters', 'kilometers', 'miles', 'feet' |

📌 C – Функции Tableau 📌

| Функция    | **Синтаксис**                                                         | **Вывод**                   | **Определение (RU)**                                                        | **Пример**                                                                                  | **Примечания (RU)**                               |
| ---------  | --------------------------------------------------------------------- | --------------------------- | --------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- | ------------------------------------------------- |
| CASE       | `CASE <expression> WHEN <value> THEN <then> ... [ELSE <default>] END` | Зависит от типа данных then | Проверяет выражение по списку значений, возвращая соответствующий результат | `CASE [Season] WHEN 'Summer' THEN 'Sandals' WHEN 'Winter' THEN 'Boots' ELSE 'Sneakers' END` | Используется с WHEN, THEN, ELSE, END. Аналог `IF` |
| CEILING    | `CEILING(number)`                                                     | Целое                       | Округляет число вверх до ближайшего целого                                  | `CEILING(2.1) = 3`                                                                          | См. также `FLOOR`, `ROUND`                        |
| CHAR       | `CHAR(number)`                                                        | Строка                      | Возвращает символ по коду ASCII                                             | `CHAR(65) = 'A'`                                                                            | Обратная к `ASCII`                                |
| COLLECT    | `COLLECT(spatial)`                                                    | Geometry                    | Агрегирует значения в поле пространственных данных                          |                                                                                             | Используется только с пространственными полями    |
| CONTAINS   | `CONTAINS(string, substring)`                                         | Boolean                     | Проверяет, содержит ли строка подстроку                                     | `CONTAINS("Calculation", "alcu") = true`                                                    | См. также `IN`, поддерживает RegEx                |
| CORR       | `CORR(expr1, expr2)`                                                  | Число (-1 до 1)             | Возвращает коэффициент корреляции Пирсона между двумя выражениями           | `{CORR(Sales, Profit)}`                                                                     | Квадрат = R², можно использовать с LOD            |
| COS        | `COS(number)`                                                         | Число                       | Возвращает косинус угла в радианах                                          | `COS(PI() / 4) = 0.7071...`                                                                 | Обратная: `ACOS`                                  |
| COT        | `COT(number)`                                                         | Число                       | Возвращает котангенс угла в радианах                                        | `COT(PI() / 4) = 1`                                                                         | См. также `ATAN`, `TAN`                           |
| COUNT      | `COUNT(expression)`                                                   | Целое                       | Считает количество элементов (NULL не считаются)                            |                                                                                             | -                                                 |
| COUNTD     | `COUNTD(expression)`                                                  | Целое                       | Считает количество различных значений                                       |                                                                                             | -                                                 |
| COVAR      | `COVAR(expr1, expr2)`                                                 | Число                       | Возвращает выборочную ковариацию двух выражений                             |                                                                                             | Для выборочных данных                             |
| COVARP     | `COVARP(expr1, expr2)`                                                | Число                       | Возвращает ковариацию для генеральной совокупности                          |                                                                                             | Для всех элементов совокупности                   |

📌 D – Функции Tableau 📌

| Функция     | **Синтаксис**                        | **Вывод**       | **Определение (RU)**                           | **Пример**                                     | **Примечания (RU)**                      |
| ----------  | ------------------------------------ | --------------- | ---------------------------------------------- | ---------------------------------------------- | ---------------------------------------- |
| DATE        | `DATE(expression)`                   | Дата            | Преобразует строку/число в дату                | `DATE("9/22/2018")`                            | Автоопределение формата, см. `DATEPARSE` |
| DATEADD     | `DATEADD(date_part, interval, date)` | Дата            | Добавляет интервал к дате                      | `DATEADD('day', 7, [Due Date])`                | Поддержка ISO 8601                       |
| DATEDIFF    | `DATEDIFF(date_part, date1, date2)`  | Целое           | Разница между датами в указанных единицах      | `DATEDIFF('day', #3/25/1986#, #2/20/2021#)`    | Поддержка ISO 8601                       |
| DATENAME    | `DATENAME(date_part, date)`          | Строка          | Возвращает имя части даты                      | `DATENAME('month', #1986-03-25#) = "March"`    | См. также `DATEPART`                     |
| DATEPARSE   | `DATEPARSE(format, string)`          | Дата            | Преобразует строку в дату по заданному формату | `DATEPARSE('yyyy-MM-dd', "1986-03-25")`        | Для специфичных форматов                 |
| DATEPART    | `DATEPART(date_part, date)`          | Целое           | Возвращает часть даты числом                   | `DATEPART('month', #1986-03-25#) = 3`          | Быстрее, чем `DATENAME`                  |
| DATETIME    | `DATETIME(expression)`               | Дата-время      | Возвращает дату и время                        | `DATETIME("April 15, 2005 07:59:00")`          | -                                        |
| DATETRUNC   | `DATETRUNC(date_part, date)`         | Дата            | Усекание даты до указанной точности            | `DATETRUNC('month', #9/22/2018#) = #9/1/2018#` | -                                        |
| DAY         | `DAY(date)`                          | Целое           | Возвращает день месяца                         | `DAY(#9/22/2018#) = 22`                        | См. также `MONTH`, `YEAR`                |
| DEGREES     | `DEGREES(number)`                    | Число (градусы) | Преобразует радианы в градусы                  | `DEGREES(PI()/4) = 45`                         | Обратная: `RADIANS`                      |
| DISTANCE    | `DISTANCE(point1, point2, 'units')`  | Число           | Расстояние между двумя точками                 | `DISTANCE([Point1], [Point2], 'km')`           | Единицы: meters, km, miles, feet         |
| DIV         | `DIV(int1, int2)`                    | Целое           | Возвращает целую часть от деления              | `DIV(11, 2) = 5`                               | -                                        |
| DOMAIN      | `DOMAIN(string_url)`                 | -               | Возвращает домен URL (только для BigQuery)     |                                                | Только BigQuery                          |

📌 E – Функции Tableau 📌

| Функция    | **Синтаксис**                 | **Вывод** | **Определение (RU)**                                            | **Пример**                                                | **Примечания (RU)**                                  |
| ---------- | ----------------------------- | --------- | --------------------------------------------------------------- | --------------------------------------------------------- | ---------------------------------------------------- |
| ELSE       | Используется в `IF`, `CASE`   | -         | Задает значение по умолчанию, если ни одно условие не выполнено | `IF [Season]="Summer" THEN "Sandals" ELSE "Sneakers" END` | Не требует условия, возвращает значение по умолчанию |
| ELSEIF     | `ELSEIF <test> THEN <then>`   | -         | Дополнительное условие в конструкции `IF`                       | `IF ... ELSEIF [Season]="Winter" THEN "Boots"`            | Используется вместе с `IF`, `THEN`, `ELSE`, `END`    |
| END        | Завершает `IF` / `CASE`       | -         | Завершает конструкцию `IF` или `CASE`                           | `IF ... THEN ... ELSE ... END`                            | Закрывает условные конструкции                       |
| ENDSWITH   | `ENDSWITH(string, substring)` | Boolean   | Проверяет, оканчивается ли строка подстрокой                    | `ENDSWITH("Tableau", "eau") = TRUE`                       | Игнорирует пробелы в конце                           |
| EXCLUDE    | -                             | -         | LOD выражение: исключает из детализации                         | `{EXCLUDE [Region]: SUM([Sales])}`                        | См. LOD Expressions                                  |
| EXP        | `EXP(number)`                 | Число     | Возвращает e в степени числа                                    | `EXP(2) = 7.389`                                          | См. также `LN`                                       |

📌 F – Функции Tableau 📌

| Функция    | **Синтаксис**                            | **Вывод**                 | **Определение (RU)**                        | **Пример**                           | **Примечания (RU)**          |
| -----------| ---------------------------------------- | ------------------------- | ------------------------------------------- | ------------------------------------ | ---------------------------- |
| FIND       | `FIND(string, substring, [start])`       | Число                     | Возвращает позицию подстроки в строке       | `FIND("Calculation", "alcu") = 2`    | Первый символ = 1            |
| FINDNTH    | `FINDNTH(string, substring, occurrence)` | Число                     | Позиция n-го вхождения подстроки            | `FINDNTH("Calculation", "a", 2) = 7` | Не на всех источниках        |
| FIRST      | `FIRST()`                                | Число                     | Смещение текущей строки относительно первой |                                      | Table Calculation            |
| FIXED      | -                                        | -                         | LOD выражение: фиксирует детализацию        | `{FIXED [Category]: SUM([Sales])}`   | См. LOD Expressions          |
| FLOAT      | `FLOAT(expression)`                      | Число с плавающей запятой | Приведение к типу float                     | `FLOAT(3) = 3.000`                   | См. также `INT`              |
| FLOOR      | `FLOOR(number)`                          | Целое                     | Округляет вниз до целого                    | `FLOOR(7.9) = 7`                     | См. также `CEILING`, `ROUND` |
| FULLNAME   | `FULLNAME()`                             | Строка                    | Возвращает полное имя текущего пользователя | `FULLNAME()`                         | Для фильтров по пользователю |

📌 G – Функции Tableau 📌

| Функция             | **Синтаксис**                             | **Вывод** | **Определение (RU)**              | **Пример** | **Примечания (RU)**    |
| ------------------- | ----------------------------------------- | --------- | --------------------------------- | ---------- | ---------------------- |
| GET\_JSON\_OBJECT   | `GET_JSON_OBJECT(JSON_string, JSON_path)` | Строка    | Возвращает элемент JSON по пути   |            | Только Hadoop Hive     |
| GROUP\_CONCAT       | `GROUP_CONCAT(expression)`                | Строка    | Объединяет значения в одну строку |            | Только Google BigQuery |

📌 H – Функции Tableau 📌

| Функция   | **Синтаксис**      | **Вывод** | **Определение (RU)**                         | **Пример**                                 | **Примечания (RU)**     |
| ----------| ------------------ | --------- | -------------------------------------------- | ------------------------------------------ | ----------------------- |
| HEXBINX   | `HEXBINX(x, y)`    | Число     | Возвращает X-координату ближайшего гексагона | `HEXBINX([Longitude]*2.5, [Latitude]*2.5)` | Для построения hex-карт |
| HEXBINY   | `HEXBINY(x, y)`    | Число     | Возвращает Y-координату ближайшего гексагона | `HEXBINY([Longitude]*2.5, [Latitude]*2.5)` | -                       |
| HOST      | `HOST(string_url)` | Строка    | Возвращает хост из URL                       |                                            | Только Google BigQuery  |

📌 I – Функции Tableau 📌

| Функция        | **Синтаксис**                             | **Вывод**       | **Определение (RU)**                            | **Пример**                                                | **Примечания (RU)**                  |
| -------------- | ----------------------------------------- | --------------- | ----------------------------------------------- | --------------------------------------------------------- | ------------------------------------ |
|  IF            | `IF <test> THEN <then> [ELSE <else>] END` | Зависит от then | Условная логика                                 | `IF [Season]="Summer" THEN "Sandals" ELSE "Sneakers" END` | Используется с `THEN`, `ELSE`, `END` |
|  IFNULL        | `IFNULL(expr1, expr2)`                    | Тип expr        | Если expr1 не NULL, возвращает его, иначе expr2 | `IFNULL([Room], "TBD")`                                   | Для обработки NULL                   |
|  IIF           | `IIF(test, then, else, [unknown])`        | Зависит от then | Проверяет условие, возвращая then или else      | `IIF([Season]='Summer', 'Sandals', 'Other')`              | Альтернатива `IF`                    |
|  IN            | `<expr1> IN <expr2>`                      | Boolean         | Проверяет наличие значения в списке/наборе      | `SUM([Cost]) IN (1000, 15, 200)`                          | Используется с наборами              |
|  INCLUDE       | -                                         | -               | LOD выражение: включает детализацию             | `{INCLUDE [Category]: SUM([Sales])}`                      | См. LOD Expressions                  |
|  INDEX         | `INDEX()`                                 | Число           | Индекс текущей строки                           |                                                           | Table Calculation                    |
|  INT           | `INT(expression)`                         | Целое           | Приведение к типу integer                       | `INT(8/3) = 2`                                            | См. также `FLOAT`                    |
|  INTERSECTS    | `INTERSECTS(geometry1, geometry2)`        | Boolean         | Проверяет пересечение геометрий                 |                                                           | Поддержка точка/линия/полигон        |
|  ISDATE        | `ISDATE(string)`                          | Boolean         | Проверяет, является ли строка датой             | `ISDATE("9/22/2018") = TRUE`                              | Только для строк                     |
|  ISFULLNAME    | `ISFULLNAME("Full Name")`                 | Boolean         | Проверяет имя текущего пользователя             | `ISFULLNAME("Hamlin Myrer")`                              | Для фильтров пользователей           |
|  ISMEMBEROF    | `ISMEMBEROF("Group Name")`                | Boolean         | Проверяет членство пользователя в группе        | `ISMEMBEROF("Superstars")`                                | Tableau Server/Cloud                 |
|  ISNULL        | `ISNULL(expression)`                      | Boolean         | Проверяет, является ли значение NULL            | `ISNULL([Room])`                                          | Сравни с `IFNULL`                    |
|  ISOQUARTER    | `ISOQUARTER(date)`                        | Целое           | Возвращает квартал по ISO                       | `ISOQUARTER(#1986-03-25#) = 1`                            | См. также `ISOYEAR`                  |
|  ISOWEEK       | `ISOWEEK(date)`                           | Целое           | Возвращает неделю по ISO                        | `ISOWEEK(#1986-03-25#) = 13`                              | -                                    |
|  ISOWEEKDAY    | `ISOWEEKDAY(date)`                        | Целое           | День недели по ISO (1-7)                        | `ISOWEEKDAY(#1986-03-25#) = 2`                            | -                                    |
|  ISOYEAR       | `ISOYEAR(date)`                           | Целое           | Возвращает год по ISO                           | `ISOYEAR(#1986-03-25#) = 1986`                            | -                                    |
|  ISUSERNAME    | `ISUSERNAME("username")`                  | Boolean         | Проверяет имя пользователя                      | `ISUSERNAME("hmyrer")`                                    | Для фильтров пользователей           |

📌 L – Функции Tableau 📌

| Функция     | **Синтаксис**                  | **Вывод**            | **Определение (RU)**                             | **Пример**                        | **Примечания**           |
| ------------| ------------------------------ | -------------------- | ------------------------------------------------ | --------------------------------- | ------------------------ |
| LAST        | `LAST()`                       | Число                | Смещение строки относительно последней строки    |                                   | Table Calculation        |
| LEFT        | `LEFT(string, number)`         | Строка               | Возвращает n левых символов строки               | `LEFT("Matador", 4) = "Mata"`     | См. также `MID`, `RIGHT` |
| LEN         | `LEN(string)`                  | Число                | Длина строки                                     | `LEN("Matador") = 7`              | Не путать с `LENGTH`     |
| LENGTH      | `LENGTH(geometry, 'units')`    | Число                | Длина линии/геометрии в указанных единицах       | `LENGTH([Spatial], 'meters')`     | Не путать с `LEN`        |
| LN          | `LN(number)`                   | Число                | Натуральный логарифм числа                       | `LN(50) = 3.912`                  | См. `LOG`, `EXP`         |
| LOG         | `LOG(number, [base])`          | Число                | Логарифм по основанию (по умолчанию 10)          | `LOG(16,4) = 2`                   | См. `LN`, `POWER`        |
| LOG2        | `LOG2(number)`                 | Число                | Логарифм по основанию 2                          |                                   | Только Google BigQuery   |
| LOOKUP      | `LOOKUP(expression, [offset])` | Зависит от выражения | Возвращает значение из другой строки по смещению |                                   | Table Calculation        |
| LOWER       | `LOWER(string)`                | Строка               | Переводит строку в нижний регистр                | `LOWER("Product") = "product"`    | См. `UPPER`, `PROPER`    |
| LTRIM       | `LTRIM(string)`                | Строка               | Удаляет пробелы слева                            | `LTRIM(" Matador ") = "Matador "` | См. `RTRIM`, `TRIM`      |
| LTRIM\_THIS | `LTRIM_THIS(string, string)`   | Строка               | Удаляет указанные символы слева                  |                                   | Только Google BigQuery   |

📌 M – Функции Tableau 📌

| Функция           | **Синтаксис**                                         | **Вывод**       | **Определение (RU)**                      | **Пример**                                           | **Примечания**                             |
| ------------------| ----------------------------------------------------- | --------------- | ----------------------------------------- | ---------------------------------------------------- | ------------------------------------------ |
| MAKEDATE          | `MAKEDATE(year, month, day)`                          | Дата            | Создает дату из года, месяца и дня        | `MAKEDATE(1986,3,25)`                                | Если дата некорректна, корректируется      |
| MAKEDATETIME      | `MAKEDATETIME(date, time)`                            | Дата-время      | Объединяет дату и время в datetime        | `MAKEDATETIME("1899-12-30", #07:59:00#)`             | Только MySQL/Amazon Aurora                 |
| MAKELINE          | `MAKELINE(point1, point2)`                            | Геометрия       | Создает линию между двумя точками         |                                                      | Для карт перемещений                       |
| MAKEPOINT         | `MAKEPOINT(lat, lon, [SRID])`                         | Геометрия       | Создает точку из координат                | `MAKEPOINT(48.5, -123.1)`                            | Используется для пространственных расчетов |
| MAKETIME          | `MAKETIME(hour, minute, second)`                      | Дата-время      | Создает время (с датой 1/1/1899)          | `MAKETIME(14,52,40)`                                 | Выход = datetime                           |
| MAX               | `MAX(expr)` или `MAX(expr1, expr2)`                   | Зависит от типа | Возвращает максимальное значение          | `MAX(4,7)=7`                                         | С датами - самая поздняя дата              |
| MEDIAN            | `MEDIAN(expr)`                                        | Число           | Медиана значений                          |                                                      | Только для числовых полей                  |
| MID               | `MID(string, start, [length])`                        | Строка          | Возвращает подстроку с позиции            | `MID("Calculation", 2, 5)="alcul"`                   | См. также `LEFT`, `RIGHT`                  |
| MIN               | `MIN(expr)` или `MIN(expr1, expr2)`                   | Зависит от типа | Возвращает минимальное значение           | `MIN(4,7)=4`                                         | С датами - самая ранняя дата               |
| MODEL\_PERCENTILE | `MODEL_PERCENTILE(model, target, predictors)`         | Число (0-1)     | Вероятность, что значение ≤ наблюдаемому  | `MODEL_PERCENTILE(SUM([Sales]), COUNT([Orders]))`    | Advanced Analytics                         |
| MODEL\_QUANTILE   | `MODEL_QUANTILE(model, quantile, target, predictors)` | Число           | Возвращает значение на указанном квантиле | `MODEL_QUANTILE(0.5, SUM([Sales]), COUNT([Orders]))` | Advanced Analytics                         |
| MONTH             | `MONTH(date)`                                         | Целое           | Возвращает месяц из даты                  | `MONTH(#1986-03-25#)=3`                              | См. `DAY`, `YEAR`                          |

📌 N – Функции Tableau 📌

| Функция | **Синтаксис** | **Вывод**  | **Определение (RU)** | **Пример**   | **Примечания**                |
| --------| ------------- | ---------- | -------------------- | ------------ | ----------------------------- |
| NOT     | `NOT <expr>`  | Boolean    | Логическое отрицание | `NOT [Flag]` | Используется с `IF`, `IIF`    |
| NOW     | `NOW()`       | Дата-время | Текущие дата и время | `NOW()`      | Аналог `TODAY`, но с временем |

📌 O – Функции Tableau 📌

| Функция | **Синтаксис**              | **Вывод** | **Определение (RU)**        | **Пример**                                                | **Примечания**                 |
| --------| -------------------------- | --------- | --------------------------- | --------------------------------------------------------- | ------------------------------ |
| OR      | `<expr1> OR <expr2>`       | Boolean   | Логическое ИЛИ              | `IF [Season]="Spring" OR [Season]="Fall" THEN "Sneakers"` | Использует short-circuit       |
| OUTLINE | `OUTLINE(spatial_polygon)` | Геометрия | Преобразует полигон в линии |                                                           | Для создания слоев с контурами |

📌 P – Функции Tableau 📌

| Функция**         | **Синтаксис**                  | **Вывод**       | **Определение (RU)**                       | **Пример**                                | **Примечания**               |
| ------------------| ------------------------------ | --------------- | ------------------------------------------ | ----------------------------------------- | ---------------------------- |
| PARSE\_URL        | `PARSE_URL(string, url_part)`  | Строка          | Извлекает часть URL                        |                                           | Только Cloudera Impala       |
| PARSE\_URL\_QUERY | `PARSE_URL_QUERY(string, key)` | Строка          | Возвращает значение параметра URL          |                                           | Только Cloudera Impala       |
| PERCENTILE        | `PERCENTILE(expr, number)`     | Число           | Возвращает значение на заданном процентиле | `PERCENTILE([Score], 0.9)`                | Для извлечения перцентилей   |
| PI                | `PI()`                         | Число           | Константа π = 3.14159...                   | `PI() = 3.14159`                          | Используется в тригонометрии |
| POWER             | `POWER(number, power)`         | Число           | Возводит в степень                         | `POWER(5,3)=125`                          | Аналог `^`                   |
| PREVIOUS\_VALUE   | `PREVIOUS_VALUE(expr)`         | Зависит от типа | Предыдущее значение выражения              |                                           | Table Calculation            |
| PROPER            | `PROPER(string)`               | Строка          | Каждое слово с заглавной буквы             | `PROPER("product name") = "Product Name"` | См. `LOWER`, `UPPER`         |

📌 Q – Функции Tableau 📌

| Функция | **Синтаксис**   | **Вывод** | **Определение (RU)**          | **Пример**                  | **Примечания**      |
| --------| --------------- | --------- | ----------------------------- | --------------------------- | ------------------- |
| QUARTER | `QUARTER(date)` | Целое     | Возвращает квартал даты (1-4) | `QUARTER(#1986-03-25#) = 1` | См. `MONTH`, `YEAR` |

📌 R – Функции Tableau 📌

| Функция                       | **Синтаксис**                                  | **Вывод**       | **Определение (RU)**                 | **Пример**                                         | **Примечания**           |                   |
| ----------------------------- | ---------------------------------------------- | --------------- | ------------------------------------ | -------------------------------------------------- | ------------------------ | ----------------- |
| RADIANS                       | `RADIANS(number)`                              | Число           | Преобразует градусы в радианы        | `RADIANS(180) = 3.14159`                           | Обратная: `DEGREES`      |                   |
| RANK                          | \`RANK(expr, \['asc'                           | 'desc'])\`      | Число                                | Ранг значения в наборе данных                      |                          | Table Calculation |
| RAWSQL\_BOOL/DATE/STR/etc     | `RAWSQL_*(sql_expr, args)`                     | Зависит от типа | Использование SQL-выражений напрямую |                                                    | Pass-Through Functions   |                   |
| REGEXP\_EXTRACT               | `REGEXP_EXTRACT(string, pattern)`              | Строка          | Возвращает часть строки по RegEx     |                                                    | Для шаблонных извлечений |                   |
| REGEXP\_MATCH                 | `REGEXP_MATCH(string, pattern)`                | Boolean         | Проверяет совпадение с RegEx         |                                                    | -                        |                   |
| REGEXP\_REPLACE               | `REGEXP_REPLACE(string, pattern, replacement)` | Строка          | Заменяет по RegEx                    |                                                    | -                        |                   |
| REPLACE                       | `REPLACE(string, substring, replacement)`      | Строка          | Заменяет подстроку в строке          | `REPLACE("Version 3.8","3.8","4x") = "Version 4x"` | См. `REGEXP_REPLACE`     |                   |
| RIGHT                         | `RIGHT(string, number)`                        | Строка          | Возвращает n правых символов         | `RIGHT("Calculation", 4)="tion"`                   | См. `LEFT`, `MID`        |                   |
| ROUND                         | `ROUND(number, [decimals])`                    | Число           | Округляет до знаков после запятой    | `ROUND(1/3, 2)=0.33`                               | См. `CEILING`, `FLOOR`   |                   |
| RTRIM                         | `RTRIM(string)`                                | Строка          | Удаляет пробелы справа               | `RTRIM(" Calc ")=" Calc"`                          | См. `LTRIM`, `TRIM`      |                   |
| RUNNING\_* (AVG, SUM, etc.)*\ | `RUNNING_SUM(expr)`                            | Зависит от типа | Накопительная сумма/среднее          |                                                    | Table Calculation        |                   |

📌 S – Функции Tableau 📌

| Функция                        | **Синтаксис**                            | **Вывод**       | **Определение (RU)**                          | **Пример**                            | **Примечания**       |
| -------------------------------| ---------------------------------------- | --------------- | --------------------------------------------- | ------------------------------------- | -------------------- |
| SCRIPT\_* (BOOL, STR, etc.)*\* | `SCRIPT_*()`                             | Зависит от типа | Аналитика через внешние скрипты               |                                       | Table Calculation    |
| SHAPETYPE                      | `SHAPETYPE(geometry)`                    | Строка          | Тип геометрии ("Point", "Polygon")            | `SHAPETYPE(MAKEPOINT(...)) = "Point"` | -                    |
| SIGN                           | `SIGN(number)`                           | -1, 0, 1        | Знак числа                                    | `SIGN(-50)=-1`                        | См. `ABS`            |
| SIN                            | `SIN(number)`                            | Число           | Синус угла в радианах                         | `SIN(PI()/2)=1`                       | Обратная: `ASIN`     |
| SIZE                           | `SIZE()`                                 | Целое           | Количество строк в текущем уровне детализации |                                       | Table Calculation    |
| SPACE                          | `SPACE(number)`                          | Строка          | Возвращает строку из n пробелов               | `SPACE(3)="   "`                      | -                    |
| SPLIT                          | `SPLIT(string, delimiter, token_number)` | Строка          | Разделение строки по разделителю              | `SPLIT("a-b-c","-",2)="b"`            | -                    |
| SQRT                           | `SQRT(number)`                           | Число           | Квадратный корень                             | `SQRT(25)=5`                          | См. `SQUARE`         |
| SQUARE                         | `SQUARE(number)`                         | Число           | Квадрат числа                                 | `SQUARE(5)=25`                        | См. `SQRT`           |
| STARTSWITH                     | `STARTSWITH(string, substring)`          | Boolean         | Проверяет начало строки                       | `STARTSWITH("Tableau","Tab")=TRUE`    | См. `CONTAINS`       |
| STDEV                          | `STDEV(expr)`                            | Число           | Стандартное отклонение выборки                |                                       | -                    |
| STDEVP                         | `STDEVP(expr)`                           | Число           | Стандартное отклонение популяции              |                                       | -                    |
| STR                            | `STR(expression)`                        | Строка          | Преобразует в строку                          | `STR([ID])`                           | -                    |
| SUM                            | `SUM(expression)`                        | Число           | Сумма значений                                | `SUM([Sales])`                        | Только числовые поля |

📌 T – Функции Tableau 📌

| Функция | **Синтаксис**               | **Вывод**       | **Определение (RU)**                                   | **Пример**                     | **Примечания**               |
| --------| --------------------------- | --------------- | ------------------------------------------------------ | ------------------------------ | ---------------------------- |
| TAN     | `TAN(number)`               | Число           | Тангенс угла в радианах                                | `TAN(PI()/4)=1`                | См. `ATAN`, `RADIANS`        |
| THEN    | Используется в `IF`, `CASE` | -               | Указывает возвращаемое значение при выполнении условия | `IF [x]>0 THEN "Positive" END` | Обязателен в `IF`, `CASE`    |
| TODAY   | `TODAY()`                   | Дата            | Текущая дата                                           | `TODAY()`                      | Аналог `NOW`, но без времени |
| TOTAL   | `TOTAL(expression)`         | Зависит от типа | Общая сумма по расчетам                                |                                | Table Calculation            |
| TRIM    | `TRIM(string)`              | Строка          | Убирает пробелы с обеих сторон                         | `TRIM(" a ")="a"`              | См. `LTRIM`, `RTRIM`         |

📌 U – Функции Tableau 📌

| Функция    | **Синтаксис**   | **Вывод** | **Определение (RU)**                   | **Пример**             | **Примечания**               |
| -----------| --------------- | --------- | -------------------------------------- | ---------------------- | ---------------------------- |
| UPPER      | `UPPER(string)` | Строка    | Переводит в верхний регистр            | `UPPER("calc")="CALC"` | См. `LOWER`, `PROPER`        |
| USERDOMAIN | `USERDOMAIN()`  | Строка    | Возвращает домен текущего пользователя |                        | Tableau Cloud/Server         |
| USERNAME   | `USERNAME()`    | Строка    | Имя пользователя                       | `USERNAME()="jdoe"`    | Для фильтров по пользователю |

📌 V – Функции Tableau 📌

| Функция | **Синтаксис**      | **Вывод** | **Определение (RU)** | **Пример** | **Примечания** |
| --------| ------------------ | --------- | -------------------- | ---------- | -------------- |
| VAR     | `VAR(expression)`  | Число     | Дисперсия выборки    |            | -              |
| VARP    | `VARP(expression)` | Число     | Дисперсия популяции  |            | -              |

📌 W – Функции Tableau 📌

| Функция                       | **Синтаксис**         | **Вывод**       | **Определение (RU)**            | **Пример**                     | **Примечания**      |
| ------------------------------| --------------------- | --------------- | ------------------------------- | ------------------------------ | ------------------- |
| WEEK                          | `WEEK(date)`          | Целое           | Номер недели из даты            | `WEEK(#1986-03-25#)=13`        | См. `MONTH`, `YEAR` |
| WHEN                          | Используется в `CASE` | -               | Проверка значения в `CASE`      | `WHEN 'Summer' THEN 'Sandals'` | С `CASE`, `THEN`    |
| WINDOW\_* (SUM, AVG, etc.)*\* | `WINDOW_SUM(expr)`    | Зависит от типа | Оконные (скользящие) вычисления |                                | Table Calculation   |

📌 X – Функции Tableau 📌

| Функция     | **Синтаксис**             | **Вывод**       | **Определение (RU)**              | **Пример** | **Примечания**     |
| ------------| ------------------------- | --------------- | --------------------------------- | ---------- | ------------------ |
| XPATH\_**\* | `XPATH_STRING(xml, path)` | Зависит от типа | Извлечение данных из XML по XPath |            | Только Hadoop Hive |

📌 Y – Функции Tableau 📌

| Функция | **Синтаксис** | **Вывод** | **Определение (RU)** | **Пример**                | **Примечания**     |
| --------| ------------- | --------- | -------------------- | ------------------------- | ------------------ |
| YEAR    | `YEAR(date)`  | Целое     | Год из даты          | `YEAR(#1986-03-25#)=1986` | См. `MONTH`, `DAY` |

📌 Z – Функции Tableau 📌

| Функция | **Синтаксис**    | **Вывод**       | **Определение (RU)**                       | **Пример**    | **Примечания**              |
| --------| ---------------- | --------------- | ------------------------------------------ | ------------- | --------------------------- |
| ZN      | `ZN(expression)` | Зависит от типа | Возвращает значение, если не NULL, иначе 0 | `ZN([Grade])` | Альтернатива `IFNULL` (с 0) |
